import React from 'react';
import TodoItem from './TodoItem';

const TodoList = () => {
  return (
    <section>
      <h1>Todo List</h1>
      <TodoItem/>
    </section>
  );
};

export default TodoList;
